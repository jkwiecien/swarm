// Global Vitest setup, loaded before every test file (see vitest.config.ts).
// Intentionally empty for now — a home for shared test bootstrapping (env
// stubs, global mocks) as the suite grows. Mirrors Cascade's tests/setup.ts.
export {};
