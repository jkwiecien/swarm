# cp .env.example .env, then set this to the same value as DASHBOARD_TOKEN in the
# repo root .env — Vite only exposes VITE_-prefixed vars to client code, so the
# token needs its own copy here under that prefix.
VITE_DASHBOARD_TOKEN=
