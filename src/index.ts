// Placeholder entry point. The real router/worker wiring lands in later tasks
// (SWARM-2 onward); for now this just proves the toolchain builds and runs.
function main(): void {
	console.log('swarm: toolchain scaffold ready');
}

main();
