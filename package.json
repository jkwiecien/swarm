{
	"name": "swarm",
	"version": "0.1.0",
	"description": "Local-First, Federated Multi-Agent Framework for automating software engineering workflows",
	"type": "module",
	"main": "dist/index.js",
	"scripts": {
		"dev": "node --env-file=.env --import tsx/esm --watch src/index.ts",
		"dev:router": "node --env-file=.env --import tsx/esm --watch src/router/index.ts",
		"dev:worker": "node --env-file=.env --import tsx/esm --watch src/worker/index.ts",
		"build": "tsc",
		"start": "node dist/index.js",
		"test": "vitest run --project unit",
		"test:unit": "vitest run --project unit",
		"test:integration": "vitest run --project integration",
		"test:all": "vitest run",
		"test:watch": "vitest --project unit",
		"test:coverage": "vitest run --project unit --coverage",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"typecheck": "tsc --noEmit",
		"verify": "npm run lint && npm run typecheck && npm test",
		"prepare": "lefthook install"
	},
	"keywords": [
		"ai",
		"llm",
		"agents",
		"automation",
		"github",
		"local-first"
	],
	"author": "Jacek Kwiecien",
	"license": "MIT",
	"engines": {
		"node": ">=22.0.0"
	},
	"dependencies": {
		"zod": "^3.25.0"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.10",
		"@commitlint/cli": "^20.1.0",
		"@commitlint/config-conventional": "^20.0.0",
		"@types/node": "^22.10.2",
		"@vitest/coverage-v8": "^3.2.4",
		"lefthook": "^1.10.10",
		"tsx": "^4.19.2",
		"typescript": "^5.7.2",
		"vitest": "^3.2.4"
	}
}
